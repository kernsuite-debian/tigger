#!/usr/bin/make -f
# -*- makefile -*-
#
export DH_VERBOSE=1
export PYBUILD_NAME=tigger

%:
	dh $@ --with python2 --buildsystem=pybuild

override_dh_auto_clean:
	python setup.py clean
	rm -rf .pybuild
	rm -rf *.egg-info
	rm -rf dist/*
	rm -rf build

